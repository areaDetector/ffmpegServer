#RELEASE Location of external products
# Run "gnumake clean uninstall install" in the application
# top directory each time this file is changed.

TEMPLATE_TOP=$(EPICS_BASE)/templates/makeBaseApp/top
SUPPORT=/dls_sw/prod/R3.14.11/support
WORK=/dls_sw/work/R3.14.11/support

# If you don't want to install into $(TOP) then
# define INSTALL_LOCATION_APP here
#INSTALL_LOCATION_APP=<fullpathname>

#If using the sequencer, point SNCSEQ at its top directory:
#SNCSEQ=/corvette/home/epics/devel/seq/2-0-11

# ASYN is needed for sockets and for smartControl
ASYN=$(SUPPORT)/asyn/4-12dls1


# CALC is needed for scalcout record that SSCAN uses
CALC=$(SUPPORT)/calc/2-7dls1

# SSCAN is needed for scanning threshold on Pilatus
SSCAN=$(SUPPORT)/sscan/2-6-5dls1

# BUSY is needed for busy record
BUSY=$(SUPPORT)/busy/1-3

# MCA is needed for fast accumulation of ROI counts
MCA=$(SUPPORT)/mca/6-11dls2

#AUTOSAVE is needed for save_restore
AUTOSAVE=$(SUPPORT)/autosave/4-6dls1

#AREA_DETECTOR=$(TOP)
AREADETECTOR=$(SUPPORT)/areaDetector/1-5dls1
FFMPEG=$(SUPPORT)/ffmpeg/1-1
FFMPEGSERVER=$(WORK)/ffmpegServer

# EPICS_BASE usually appears last so other apps can override stuff:
EPICS_BASE=/dls_sw/epics/R3.14.11/base

#Capfast users may need the following definitions
#CAPFAST_TEMPLATES=
#SCH2EDIF_PATH=
